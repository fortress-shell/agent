---
machine:
  environment:
    CIRCLE_ENV: test
# dependencies:
#   override:
#     - npm install coffeescript
#     - gem uninstall bundler
#     - gem install bundler --pre
general:
  branches:
    only:
      - master
      - /feature-.*/
test:
  override:
    - phpunit test/unit-tests
# deployment:
#   override:
#     - phpunit test/unit-tests
