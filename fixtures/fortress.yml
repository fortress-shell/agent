---
machine:
  environment:
    CIRCLE_ENV: test
general:
  branches:
    only:
      - master
      - /feature-.*/
test:
  override:
    - echo $CIRCLE_ENV

# dependencies:
#   override:
#     - npm install coffeescript
#     - gem uninstall bundler
#     - gem install bundler --pre
# deployment:
#   override:
#     - phpunit test/unit-tests
